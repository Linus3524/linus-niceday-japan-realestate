/**
 * 站內知識庫搜尋的共用比對邏輯。
 *
 * 兩個核心行為：
 * 1. 多關鍵字：查詢會先切詞，每個詞都要命中才算（AND，順序不拘）。
 *    原本是整串 `text.includes(query)`，空白也被當成比對字元，
 *    所以「敷金 禮金」這種查法一定查無資料。
 * 2. 同義詞：台灣說法、日文漢字、日文原文互相對照，
 *    使用者打「頭期款」也能找到寫成「自備款」的內容。
 */

/**
 * 同義詞群組：同一組內的詞彼此等價，查任一個都會找到其他寫法。
 *
 * 維護原則：
 * - 只放「講的是同一件事」的詞。制度上不同的東西不要放在一起
 *   （例如「手付金」是簽約時付的訂金，「頭期款」是自備款，兩者不同組）。
 * - 台灣慣用語對應到日本制度時，若只是「功能相近」而非完全相等，
 *   仍可放進來幫助搜尋，但不要因此改寫內文的正式用詞。
 * - 新增知識庫內容時，如果用了新的專有名詞，記得回來補一組。
 */
const SYNONYM_GROUPS: string[][] = [
  // ── 租屋：初期費用 ──
  ["敷金", "押金", "保證金", "保証金", "しききん"],
  ["禮金", "礼金", "れいきん"],
  ["仲介費", "仲介手数料", "仲介手續費", "仲介服務費", "服務費", "中介費", "中介", "地產代理", "經紀"],
  ["管理費", "共益費"],
  ["更新料", "續約金", "更新費", "續租費", "更新租約", "租約更新", "續約", "續租", "再契約料", "再契約手續費", "再契約手数料", "再契約"],
  ["定期借家", "定期借家契約", "定期租賃", "定期租約", "定期契約"],
  ["普通借家", "普通借家契約", "普通租約", "一般租約"],
  ["借地借家法", "借地借家", "日本租賃法", "租賃法"],
  ["初期費用", "初期費", "前期費用", "入住費用", "簽約費用"],
  ["火災保險", "火災保険", "住宅保險", "住宅火險"],
  ["清掃費", "清潔費", "定額清掃費", "ハウスクリーニング"],
  ["鑰匙更換費", "鍵交換費", "換鎖費", "換鑰匙"],
  ["房租", "家賃", "租金", "月租"],
  ["租金行情", "房租行情", "租屋行情", "家賃相場", "租金相場"],
  ["租金調漲", "調漲租金", "漲租", "漲房租", "租金上漲", "賃料増額", "賃料値上げ", "家賃値上げ", "値上げ"],

  // ── 租屋：申請與審查 ──
  ["審查", "審査", "審核"],
  ["保證公司", "保証会社", "保證會社", "保証公司"],
  ["連帶保證人", "連帯保証人", "保證人", "保証人"],
  ["在留卡", "居留卡", "在留カード"],
  ["在留資格", "簽證", "ビザ"],
  ["打工度假", "打工渡假", "打工簽證", "度假簽證", "ワーキングホリデー", "working holiday"],
  ["源泉徵收票", "源泉徴収票", "源泉票", "扣繳憑單"],
  ["內見", "看房", "賞屋", "內覽", "看屋"],
  ["先行契約", "未內見契約", "不內見簽約", "不看房簽約", "不內見"],
  ["先行申請", "未內見申請", "不內見申請", "先申請後內見"],
  ["看得到租不到", "看到卻租不到", "網站房源", "過期物件", "釣魚物件", "suumo", "homes"],
  ["物件", "房源", "房子", "房屋"],
  ["房東", "屋主", "業主", "出租人", "大家"],
  ["網路", "網絡", "網路回線", "光纖", "wifi", "wi-fi", "インターネット"],

  // ── 租屋：退租 ──
  ["退租", "解約", "退房", "搬離"],
  ["原狀回復", "原状回復", "原狀恢復", "原状恢復", "回復原狀", "恢復原狀", "復原"],
  ["解約預告", "解約予告", "退租通知", "提前告知"],
  ["立ち退き", "立退き", "立ち退き要求", "逼退", "被趕走", "要求搬走", "退去要求", "強制搬遷"],
  ["立退料", "立ち退き料", "退去費用", "搬遷補償", "搬家補償", "補償金"],
  ["オーナーチェンジ", "房東換人", "屋主變更", "業主變更", "換房東", "所有權移轉"],

  // ── 買房：價金與費用 ──
  ["手付金", "訂金", "定金"],
  ["頭期款", "自備款", "首付"],
  ["房貸", "貸款", "住宅ローン", "ローン", "融資", "房屋貸款", "按揭"],
  ["修繕積立金", "積立金", "修繕基金", "公共基金"],
  ["買房", "購屋", "置產", "買日本房", "購買房產", "購入物件"],
  ["殺價", "砍價", "議價", "講價", "値引き", "値下げ交渉"],
  ["外國人", "海外人士", "非居住者", "海外買方"],

  // ── 買房：稅費 ──
  ["印紙稅", "印紙税", "印花稅"],
  ["登錄免許稅", "登録免許税", "登記稅", "登記規費"],
  ["不動產取得稅", "不動産取得税", "契稅"],
  ["固定資產稅", "固定資産税", "房屋稅", "地價稅"],
  ["都市計畫稅", "都市計画税"],
  ["讓渡所得", "譲渡所得", "出售利益", "財產交易所得"],
  ["相續稅", "相続税", "遺產稅"],
  ["確定申告", "報稅", "申報所得"],

  // ── 買房：面積與物件型態 ──
  ["專有面積", "專有部分", "專有部份", "室內坪數", "坪數", "實用面積", "建築面積", "呎價"],
  ["壁芯面積", "壁心面積", "壁芯"],
  ["內法面積", "内法面積", "內法"],
  ["中古屋", "中古", "二手屋", "老屋", "中古物件"],
  ["新築", "新屋", "新成屋", "新建案", "預售屋", "樓花", "新盤"],
  ["一戶建", "一戸建", "透天", "透天厝", "獨棟"],
  ["公寓大樓", "マンション", "大樓", "電梯大樓"],
  ["木造公寓", "アパート"],

  // ── 買房：流程與角色 ──
  ["司法書士", "代書", "地政士"],
  ["宅建士", "宅地建物取引士", "不動產經紀人"],
  ["重要事項說明", "重要事項説明", "重説"],
  ["買付申込書", "出價單", "購買申請書"],
  ["民泊", "民宿", "短租", "airbnb"],
  ["合租", "朋友合租", "朋友同住", "室友", "ルームシェア", "二人入居", "2人入居", "兩人入住", "多人入住"],

  // ── 地名：車站推薦文用 ──
  // 「辻」不在常用中文輸入法字庫，貼文本身也特地標了讀音，
  // 因此把假名與所在地一併納入，讓打不出漢字的人也搜得到。
  ["辻堂", "つじどう", "辻堂駅"],
  ["湘南", "湘南海岸", "湘南地區"],
  ["藤澤", "藤沢", "藤澤市", "藤沢市"],

  // ── 投報、生活與設施 ──
  ["利回り", "投報率", "報酬率", "收益率"],
  ["水電", "光熱費", "水道光熱費", "電費", "瓦斯費", "水電煤", "水電瓦斯", "電力瓦斯", "電力", "瓦斯", "煤氣", "燃氣"],
  ["寵物", "可養寵物", "ペット", "ペット可"],
  ["格局", "套房", "1k", "1r", "1ldk", "2ldk"],
];

/**
 * 台日漢字與常見異體字正規化映射表。
 * 將日文常用漢字／新字體、簡體字與常見異體字歸一化至繁體中文標準字，
 * 確保「海外審查」必中「海外審査」、「印紙税」必中「印紙稅」、「一戸建」必中「一戶建」。
 */
const KANJI_NORMALIZATION_MAP: Record<string, string> = {
  // 核心台日異體字（不動產關鍵字）
  "査": "查",
  "税": "稅",
  "戸": "戶", "户": "戶",
  "証": "證", "证": "證",
  "説": "說", "说": "說",
  "価": "價", "价": "價",
  "専": "專", "专": "專",
  "気": "氣", "气": "氣",
  "験": "驗", "验": "驗",
  "渋": "澀", "澁": "澀", "涩": "澀",
  "浜": "濱", "滨": "濱",
  "横": "橫",
  "条": "條",
  "対": "對", "对": "對",
  "転": "轉", "转": "轉",
  "満": "滿", "满": "滿",
  "広": "廣", "广": "廣",
  "総": "總", "总": "總",
  "関": "關", "关": "關",
  "鉄": "鐵", "铁": "鐵",
  "区": "區",
  "県": "縣", "县": "縣",
  "権": "權", "权": "權",
  "売": "賣", "卖": "賣",
  "済": "濟", "济": "濟",
  "続": "續", "续": "續",
  "絵": "繪", "绘": "繪",
  "経": "經", "经": "經",
  "営": "營", "营": "營",
  "読": "讀", "读": "讀",
  "単": "單", "单": "單",
  "点": "點",
  "数": "數",
  "実": "實", "实": "實",
  "楽": "樂", "乐": "樂",
  "薬": "藥", "药": "藥",
  "銭": "錢", "钱": "錢",
  "積": "積", "积": "積",
  "貸": "貸", "贷": "貸",
  "賃": "賃", "赁": "賃",
  "約": "約", "约": "約",
  "規": "規", "规": "規",
  "則": "則", "则": "則",
  "円": "圓", "圆": "圓",
};

const KANJI_REGEX = new RegExp("[" + Object.keys(KANJI_NORMALIZATION_MAP).join("") + "]", "g");

/**
 * 將文字進行漢字字元正規化（轉為小寫並映射異體字／日文新字體）。
 */
export function normalizeSearchText(text: string): string {
  if (!text) return "";
  return text
    .toLocaleLowerCase()
    .replace(KANJI_REGEX, ch => KANJI_NORMALIZATION_MAP[ch] || ch);
}

/** 詞 → 該詞所屬群組的所有寫法（都存小寫並正規化漢字，比對時才不分大小寫與台日字體） */
const synonymIndex = new Map<string, Set<string>>();
for (const group of SYNONYM_GROUPS) {
  const normalized = group.map(term => normalizeSearchText(term));
  for (const term of normalized) {
    const bucket = synonymIndex.get(term) ?? new Set<string>();
    normalized.forEach(alias => bucket.add(alias));
    synonymIndex.set(term, bucket);
  }
}

/** 把查詢字串切成多個關鍵字：半形空白、全形空白、逗號與頓號都視為分隔符，並自動進行漢字歸一化 */
export function tokenizeQuery(query: string): string[] {
  return normalizeSearchText(query)
    .trim()
    .split(/[\s　、,，]+/)
    .filter(Boolean);
}

/** 把一個關鍵字展開成「它自己 ＋ 所有同義寫法」（全部經過漢字歸一化） */
export function expandToken(token: string): string[] {
  const normalized = normalizeSearchText(token);
  const bucket = synonymIndex.get(normalized);
  return bucket ? Array.from(bucket) : [normalized];
}

/**
 * 從自然語句中找出知識庫已知的搜尋概念。
 *
 * AI 顧問收到的是「退房時被扣清潔費合理嗎？」這種完整句子，不能像搜尋框一樣
 * 只靠空白切詞。這裡會辨識句中的同義詞，並讓同一群同義詞只回傳一次。
 */
export function extractKnownSearchTokens(text: string): string[] {
  const normalized = normalizeSearchText(text);
  const aliases = Array.from(synonymIndex.keys()).sort((a, b) => b.length - a.length);
  const seenGroups = new Set<string>();
  const tokens: string[] = [];

  for (const alias of aliases) {
    if (!normalized.includes(alias)) continue;
    const group = synonymIndex.get(alias);
    if (!group) continue;
    const groupKey = Array.from(group).sort().join("\u0000");
    if (seenGroups.has(groupKey)) continue;
    seenGroups.add(groupKey);
    tokens.push(alias);
  }

  return tokens;
}

/**
 * 所有關鍵字都要命中才算（AND）；單一關鍵字只要命中任一同義寫法即可（OR）。
 * 搜尋文字與被搜尋內容皆經過漢字歸一化，支援同字不同編碼的無縫比對。
 */
export function matchesAllTokens(haystack: string, tokens: string[]): boolean {
  if (tokens.length === 0) return false;
  const text = normalizeSearchText(haystack);
  return tokens.every(token => {
    const normalizedToken = normalizeSearchText(token);
    return expandToken(normalizedToken).some(alias => text.includes(alias));
  });
}

/** 把一個項目的多個欄位串成一份可搜尋的文字（略過 undefined 與空值） */
export function buildHaystack(...parts: Array<string | string[] | undefined | null>): string {
  return parts
    .flat()
    .filter((part): part is string => typeof part === "string" && part.length > 0)
    .join(" ");
}
